// const s3 = new AWS.S3()
/*
const documentClient = new AWS.DynamoDB.DocumentClient()
AWS.config.update({ region: 'us-east-1' })
*/
const example = 'new AWS.S3().getObject(params).promise()'
console.log(example)
